// Running JavaScript in Node.js executes code using the Node runtime
// and displays output in the terminal. Browser JavaScript runs inside
// a web browser and can interact with HTML and the DOM.

// REPL stands for Read-Eval-Print Loop.
// It is useful for quickly testing JavaScript code, expressions,
// and debugging without creating a separate file.

const message = "Hello from Node.js";
console.log(message);

const fruits = ["apple", "banana", "mango"];

const upper = fruits.map(f => f.toUpperCase());

console.log(upper);